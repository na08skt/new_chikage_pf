<table class="table">
  <thead>
    <tr>
      <th>title</th>
      <th>area</th>
      <th>date</th>
      <th>like</th>
      <th>comment</th>
      <th>experiences</th>
      <th>管理者公開設定</th>
      <th>投稿者公開設定</th>
    </tr>
  </thead>

  <% @location_reports.each do |location_report| %>
  <tbody>
    <tr>
      <th scope="row">
        <%= link_to "#{location_report.title}", admin_location_report_path(location_report) %>
      </th>
      <td><%= location_report.area%></td>
      <td><%= location_report.date%></td>
      <!--以下未実装項目-->
      <td><%= location_report.favorites.count %></td>
      <td><%= location_report.comments.count%></td>
      <td><%= location_report.experiences.count %></td>
      <td><%= location_report.report_status %></td>
      <td><%= location_report.publication_status %></td>
    </tr>
  </tbody>
  <% end %>
  <%= paginate @location_reports %>
</table>